document.write('\
\
<h2 id="p-busy">busy</h2>\
<p>\
This read-only property has value <span class="code">true</span> when the ticker is busy. It is either fetching a feed \
or it is displaying the items or an error message.\
</p>\
<p>\
When the ticker is stopped (<a href="#m-stopticker"><span class="code">stopTicker</span></a>) the property will be <span class="code">false</span>.\
</p>\
<p>\
Method <a href="#m-addbusylistener"><span class="code">addBusyListener</span></a> can be called to listen to changes of this property.\
</p>\
');