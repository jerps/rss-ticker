document.write('\
\
<h2 id="a-descr-or-content">descr-or-content (descrOrContent)</h2>\
<div class="apinfo">\
<div class="g11">type</div><div class="g12">numeric</div>\
<div class="g21">default</div><div class="g22">"0"</div>\
<div class="g31">min</div><div class="g32">"0"</div>\
<div class="g41">max</div><div class="g42">"3"</div>\
</div>\
<p>\
An item may have both a "description" part and a "content" part. If this attribute\'s value is <span class="code">0</span> then an info box shows \
both of them. First the description, then the content. However, if the value is <span class="code">1</span> then only the description is shown. If \
the value is <span class="code">2</span> then only the content is shown. If <span class="code">3</span> then either the description or the content \
is shown, depending on which one contains the most text.\
</p>\
');