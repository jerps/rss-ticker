document.write('\
\
<h2 id="p-running">running</h2>\
<p>\
This read-only property has value <span class="code">true</span> when a run is active, i.e. items are scrolling over the screen.\
</p>\
<p>\
When this property is <span class="code">false</span> and the ticker is not stopped (property <a href="#p-busy"><span class="code">busy</span></a> is <span class="code">true</span>) \
then the ticker is fetching a feed.\
</p>\
<p>\
Method <a href="#m-addrunninglistener"><span class="code">addRunningListener</span></a> can be called to listen to changes of this property.\
</p>\
');