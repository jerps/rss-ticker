document.write('\
\
<h2 id="a-no-imgs">no-imgs (noImgs)</h2>\
<div class="apinfo">\
<div class="g11">type</div><div class="g12">boolean</div>\
<div class="g21">default</div><div class="g22">false</div>\
</div>\
<p>\
If this attribute is set (or the property is set to <span class="code">true</span>) then no images will be fetched anymore. If the ticker is running \
and images were fetched previously then no images are shown when the next run begins.\
</p>\
<p>\
If this attribute is removed (or the property is set to <span class="code">false</span>), and no images were fetched previously, then images will be \
fetched again when the next run starts.\
</p>\
');