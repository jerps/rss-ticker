document.write('\
\
<h1 id="colors">Colors</h1>\
<p>\
For some attributes a color value must be specified. The value must be a hexadecimal RGB value, such as <span class="code">#fff</span> \
or <span class="code">#f0f0f0</span>. Attribute <a href="#a-infobox-link-color"><span class="code">infobox-link-color</span></a> can \
have an RGBA color (hexadecimal, with alpha component, for example <span class="code">#f0f0f0aa</span>).\
</p>\
<p>\
The pound sign at the beginning of a color value is optional. A color value can contain any character. Any character that is not a digit \
or an uppercase or lowercase letter is ignored. If not enough hexadecimal digits are given then the G and/or B values are assumed to be 0. \
The A value is assumed to be ff.\
</p>\
');